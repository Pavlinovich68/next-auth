import React from 'react';

const LayoutAbout = ({children}: {
    children: React.ReactNode
}) => {
    return (
        <div>
            LayoutAbout
            {children}
        </div>
    );
};

export default LayoutAbout;